# DocIngest dependencies
#
# Preferred install method (uses pyproject.toml as single source of truth):
#   pip install -e .              # Core dependencies
#   pip install -e ".[nlp]"       # + Japanese keyword extraction (SudachiPy)
#   pip install -e ".[mcp]"       # + MCP Server (FastMCP)
#   pip install -e ".[audio]"     # + Audio transcription (DashScope Qwen3-ASR)
#   pip install -e ".[nlp,mcp,audio]"  # All optional
#
# This file is for environments that require requirements.txt (e.g. Docker).
# It mirrors pyproject.toml — if you update deps, update pyproject.toml first.

# === Core ===
docling>=2.70
litellm>=1.80
typer>=0.12
pyyaml>=6.0
rich>=13.0
python-dotenv>=1.0
diskcache>=5.6
openpyxl>=3.1
pillow>=10.0
python-pptx>=1.0
beautifulsoup4>=4.12
defusedxml>=0.7
pymupdf>=1.24
requests>=2.31

# === Optional (uncomment as needed) ===
# sudachipy>=0.6            # Japanese keyword extraction
# sudachidict_core>=20240409
# fastmcp>=2.0              # MCP Server
# dashscope                 # Audio transcription (Qwen3-ASR)
# magika>=1.0               # Content-based file type detection
# yt-dlp                    # YouTube/Bilibili URL support
