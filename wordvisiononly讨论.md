# DocIngest — Word 全 Vision-only 处理：调查与实测报告

> 调查日期：2026-06-27
> 目的：探索 docx 是否能"绕过 docling、全靠页图 Vision"处理，并搞清 batch/并发的真实关系与最优喂图策略。
> 方法：全程真调 Vision（gemini-3-flash-preview）、真实 docx（19页操作手册，图文混排）、不 mock。
> 结论先行：**全 vision-only 在精度上可行；但"batch 连贯喂"被实测否定——并发单页才是精度+速度双优，而这恰是 docx 现状。**

---

## 一、背景：docx 现在到底怎么处理（先搞清现状，再谈改）

docx 进来后是 **三路并行**，不是单一路径：

```
docx
 ├─ docling 解析 → 正文文字 markdown（段落/表格的文字结构）+ 提取内嵌图
 ├─ 页图：LibreOffice 排版成 PDF → 截图成页图（每页一张，180DPI）
 │        ⚠️ 慢点在这（LibreOffice 排版）。有 triage 守门：纯文字 docx 跳过这步
 └─ Vision enrichment：
      ├─ 对页图：每页送 Vision（**当前是并发单页，不是 batch**）
      └─ 对内嵌图：每张按全分辨率单独送 Vision（细节高清）
```

**关键事实（读代码确认，非猜测）：**

1. **页图 + 内嵌图两路并存是有意设计，不是冗余。**
   - 代码注释（pipeline.py 内嵌图 enrichment）：内嵌图 `Send to Vision at full resolution`。
   - 页图是整页缩小渲染（看布局/位置），内嵌图是每张原始全分辨率（看小字细节，如账号串）。
   - 实测尺寸：内嵌图 img-009（认证弹窗）= 311×130px；整页 page = 1489×2105px，弹窗在整页里只占一小块。
   - 当初加内嵌图那路，正是因为"整页缩小后小截图里的文字会糊"——这个担心后来被实测打掉了（见三）。

2. **batch 模式只对 xlsx/xls 开，docx/pdf 不走 batch。**
   - pipeline.py `_enrich_with_vision`：`use_batched = batched_enabled and doc_format in ("xlsx","xls") and ...`
   - 所以 docx 现在是 **纯 per-page 并发**（`performance.parallel_files`=16，16路并发，每页独立一次 LLM 调用）。
   - 日志 `Vision enrichment: 16 described ... parallel=16` 指的就是 16 路并发单页，不是 batch。

3. **batch 与并发是正交的两个维度（容易混淆，厘清）：**
   | 概念 | 含义 |
   |---|---|
   | **batch** | 多页图塞进**一次** LLM 调用（一个 prompt 含 N 张图，LLM 一次连贯读） |
   | **并发(parallel)** | **多次**独立 LLM 调用同时发（线程池，每次调用读 1 页或 1 批） |
   - 两者可叠加：xlsx 现在就是"分多个 batch，batch 之间再并发"（pipeline.py:2554 `Run batches in parallel`）。

---

## 二、转图引擎实测：能不能自己快速把 word 每页转图？

docx 转图瓶颈在 **docx→PDF**（排版渲染）；PDF→图很快（fitz 0.5-0.7s）。所以加速点是"docx→PDF 用什么"。

实测对比（同一 docx，180DPI）：

| 引擎 | docx→PDF | PDF→图 | 总计 | 页数 | 备注 |
|---|---|---|---|---|---|
| **Word COM**（win32com） | 4.3s | 0.7s | **5.0s** | 19 | 真 Word 引擎，分页准 |
| LibreOffice（现状） | 9.4s | 0.5s | 9.9s | 18 | 跨平台、无 Office 依赖 |

**结论：Word COM 快 2 倍（9.9s→5.0s），且分页与真实 Word 一致（19 vs 18 页）。** 这台机器有 Word 16.0（Office 2016+），`win32com` 已装，可用。

**代价（诚实标注）：**
- Word COM 只能 **Windows + 装了 Word**；LibreOffice 跨平台、无 Office 依赖（DocIngest 要泛用，这是真实取舍）。
- COM 有进程启停开销，批量多文件时每次起 Word 较重。
- 可能与设备策略 / 其它 Word 进程冲突。

---

## 三、精度实测：单靠页图，Vision 能读准小截图里的细节吗？

**判据**：认证弹窗里的精确账号串 `PWCGLB\pwhite026`（最刁钻的细节），三种输入对比命中。

| 输入 | 尺寸 | 耗时 | 命中真值(4个细节) |
|---|---|---|---|
| A 内嵌图全分辨率（现状基线） | 311×130 | 4.9s | **4/4** |
| B 180DPI 整页（只用页图） | 1489×2105 | 8.7s | **4/4** |
| C 300DPI 整页 | 2481×3508 | 11.9s | **4/4** |

**结论：单靠整页图（连内嵌图都不要），180DPI 下 Vision 也能 4/4 读准 `PWCGLB\pwhite026`。** 之前"页图缩小看不清细节"的担心被实测推翻——现在的 Vision 模型分辨率够强。

> 边界：只测了 1 张图、1 个细节类型（英文账号串）。日文小字、密集表格、多小图同页等更难场景未单独测。

---

## 四、策略实测（核心）：并发单页 vs 大batch vs batch+并发

全 18 页、page_texts 全空（纯 vision-only，无 docling 文字）、真调 Vision。

| 策略 | 耗时 | 调用次数 | 关键探针覆盖(13) | 细探针(16) | 账号串精度 | 字数 |
|---|---|---|---|---|---|---|
| **S1 纯并发单页**（16路并发） | **18.2s** ⚡ | 18 | **13/13** | **13/16** | **PWCGLB 全对，0错字** | 34476 |
| S3 batch+并发（3批×6页） | 42.6s | 3 | 13/13 | 9/16 | **2对 + 2个错字(PWGCLB)** 🔴 | 20132 |
| S2 纯大batch（18页1次） | 49.0s | 1 | 10/13 🔴 | 6/16 | **0次（整段丢失）** 🔴 | 16516 |

**铁律式发现：一次喂的页数越多，精度越差。**
- **S2 大batch**：18页塞一次，LLM 在长输入里"概括式压缩"——认证弹窗整块丢失，Cut/Copy/Rename 菜单项也丢。
- **S3 折中**：6页/批开始出**字符级错误**——`PWCGLB` 写成 `PWGCLB`（C/G 颠倒）。肉眼确认。
- **S1 单页**：每页独立读，LLM 注意力集中在一页 → 细节最全、账号串完全正确、格式最规整（`**User name:** PWCGLB\pwhite026`）。

**为什么和"batch 更快"的直觉相反：**
之前单独测"5页batch比5次串行单页快3.3倍"——但那对比的是**串行**单页。真实系统是 **16路并发**：并发单页时 18 次调用几乎同时发出，墙钟 ≈ 最慢一页（~18s），反而比 batch 的"大请求串行/少并发"更快；且每页独立，精度最高。**batch 把多页塞一个 prompt，LLM 注意力被多页稀释 → 长输入丢细节、甚至读错字。**

---

## 五、最终结论与建议

### 可行性逐项核对
| 子问题 | 实测结论 |
|---|---|
| 页图能读准细节吗 | ✅ 180DPI 单页 4/4 命中账号串，够清晰 |
| 能自己快速转图吗 | ✅ Word COM 5s vs LibreOffice 9.9s，快 2 倍（限 Windows+Word） |
| batch 连贯喂更好吗 | ❌ **实测否定**：并发单页精度+速度双赢，batch 反而丢细节/出错字 |
| docx 现在走 batch 吗 | ❌ 不走，docx 是纯并发单页（只 xlsx 走 batch） |

### 判断
1. **"全 vision-only docx"在精度上可行**——页图够清晰，三个核心担心（细节糊/转图慢/连贯断）实测全部打掉。
2. **但"batch 连贯喂"的设想被实测推翻**——对 docx 而言，**并发单页（现状）才是精度+速度最优解**，不该改成 batch。原因：Vision 模型对单页"专注"时精度最高，多页同喂会稀释注意力、丢细节、出错字。
3. **真正值得动的优化只有一个**：用 **Word COM 替代 LibreOffice 转图**，提速 2 倍（但绑 Windows+Word，需权衡泛用性）。

### 没测到的边界（留给后续）
- 只测 1 个 docx（操作手册型，图文混排）。长正文/扫描件/复杂版式类型未测。
- "全 Vision 替代 docling 文字层"的**整篇逐字准确性 + token 成本**未系统测（本次测的是细节点命中与连贯，非整篇逐字）。
- batch 页数上限：5页 OK、6页开始出错、18页大段丢——拐点在 5-6 页之间，未精确定位。
- Word COM 批量多文件的进程开销、稳定性未压测。

---