# 三模式设计 — 测试观察（持续更新）

> 目标：为设计三种处理模式打事实基础，并落地质量地基。
> 铁律：**事实 ≠ 结论**。单次孤证不算数，弱覆盖不能当普适。逐字人工核对，不只看探针数。每个对比≥3次看稳定性。

---

## 🟢 最新结论速查（只看这个就够，2026-06-28）

> 下方「测试观察」是得出结论的过程留档；早期被推翻的观点已在原处标注作废。当前唯一有效的方向看这里 + 文末「三模式整体方案设计」。

**1. 核心质量问题已定位并落地修复**
- 病根：整页图模式下，Vision 对"密集表/含字图区"**是否逐字读全凭单次随机**（二元决策：要么全读到、要么整块当配图带过，约 50% 概率全丢）。跨 docx/PDF/pptx 三格式互证。
- prompt 微调**治标不治本**（实测命中率仅 37.5%→50%，消不掉 50% 全丢尾部）。
- **真解药 = 全分辨率单独抠图 + 强制逐字 `_EMBEDDED_IMAGE_PROMPT`**（[12,12,12] 稳定）。
- **已落地**：抠图路径从仅 docx 放开到 **docx/xlsx/pptx**。PPT 那张抖动图 [6,0,4] → 8/8 稳定。成本预检漏算 bug 也修了（PPT 抠图成本现已事前可见可审批）。xlsx 抠图上限也由 30 改 null（统一无截断，成本进预检）。

**1b. 围绕抠图的三个忧虑已实测排除（2026-06-28，详见文末「质量地基实测」）**
- **DPI 救不了抖动**：拉到 34.8MP 超清整页图，密集表满分率仍 2/8（和 180 一样）——抖动是 Vision 二元决策，非分辨率问题。业界最平衡 DPI≈200（项目默认 180，可选提到 200，锦上添花非必需）。⚠️ 代码 4MP 上限是我们自己的保守硬编码，非模型红线（Gemini 吃 34.8MP 无碍）。
- **抠图并发**：默认 16（`performance.parallel_files`），和整页 Vision 共用 worker 池 + 全局闸，不压垮 provider。
- **注入位置准确**：11 图完整 pipeline 实跑，每张转写锚定到对应 marker、内容无张冠李戴、图序=页序=注入序、unanchored=0。

**2. 三模式 = 面向场景的三预设档（平衡/准确/速度）**
- 平衡（默认）：docling + triage开 + 抠图开。
- 准确：docling + **triage全局关(每页都送Vision)** + 抠图开 + batched全关。
- 速度：vision_only（PDF/图片）。**PPT 不走 vision_only**（实测放弃，见下）；速度档对 PPT = docling + parallel_files=64 + 激进 triage。

**3. 已定/待查**
- vision_only 扩 PPT：**放弃（实测拍板，非搁置）**——耗时拆解证明 vision_only 砍的是最小一步、丢最大质量、且"64 并发"非其独家。速度档对 PPT 正解 = **docling + parallel_files=64 + 激进 triage**。详见文末。
- 速度档对 docx/xlsx 的具体路径、三档的用户入口形式：待下轮。

**4. 不做的事（参考现有保守哲学后定）**
- 不做装饰图预过滤 / 无字结果后过滤——误删含字图风险 > 收益，违背项目"宁可多花钱不丢内容"。成本已由 safety 预检兜底。

---

## 📌 进度总览（速查）

| 课题 | 状态 | 一句话结论 |
|---|---|---|
| ① batch vs per-page | ✅ 已定 | batch非通用加速;在xlsx成立=因openpyxl兜底;对docx/pdf会偷懒概括 |
| ② 各格式视觉路径核对 | ✅ 已定 | docx/xlsx原有全分辨率内嵌图路径+OOXML正文兜底→supplement;PDF/PPT当时无→full。**注:PPT已于t8补上抠图路径** |
| ③ vision_only扩docx代价 | ✅ 已定 | 不丢整篇大字;丢密集小字表(0,0,11 vs 现状12,12,12);根因=丢内嵌图路径 |
| ④ PDF: text_layer贡献 / vision_only-PDF损失 | ✅ 已定(判据修正后) | text_layer作用域窄=仅"视觉冲突纠错";vision_only-PDF损失窄而真实但不高频 |
| ⑤ PPTX: 无兜底+full现状质量 | ✅ 已定 | 含字位图[6,0,4]抖动;pptx文字多是形状(docling直读),只含字位图需读图。**→ 已由抠图路径(t8)治好** |
| 🌟 跨格式统一规律 | ✅ 已定 | 整页图模式对"密集/含字图区"是否逐字读全凭随机;全分辨率抠图路径是解药(已覆盖docx/xlsx/pptx);vision_only丢了它 |
| 🛠️ 抠图路径扩到PPT | ✅ 落地 | PPT含字图[6,0,4]→8/8稳定;成本预检漏算同步修复;回归通过 |

> ⚠️ 已踩坑教训:①一开始用整页图测内嵌图内容→假结论"密集表碰运气",回查源文件+全分辨率重测才纠正。**测前先确认"真实系统走哪条路径"。**

---

## 系统现状（已读源码确认，可信）

- **三层路由**：MediaParser(音视频) → DoclingParser(主力) → TextParser(兜底)。二进制格式Docling失败硬失败,不兜底TextParser。
- **页图来源不统一**：PDF=PyMuPDF(快); docx/xlsx/pptx=LibreOffice→PDF→截图(慢,实测docx 3-9s/xlsx 7-8s)。
- **batched只认xlsx**：`doc_format in ("xlsx","xls")`硬编码,触发条件`页/sheet≥1.5`,每批≤20图。PDF/docx/PPT永不走batch。
- **vision_only只认PDF+图片**：`_PAGE_IMAGE_FORMATS`硬编码,其它格式委托回Docling。(扩PPT已评估,搁置——见文末)
- **全分辨率抠图路径**：原仅docx/xlsx,**现已放开到pptx**(t8落地)。PDF不走(docling的PDF路径has_pil=False,无像素;矢量PDF文字层本就够)。
- **三套"跳过视觉"机制各自为政**：PDF=逐页triage / docx=skip_text_only(整文件) / xlsx=sheet_triage(逐sheet) / PPT=不跳。
- **并发模型**：per-page走ThreadPool(`parallel_files`默认16) + `_VISION_GATE`信号量(默认=parallel_files)。两道闸串联,上限16。
- **supplement vs full**：全局默认full(整页重写); xlsx翻supplement(只补视觉,openpyxl兜底表格文字)。docx/pptx/pdf都full。

## 已有的"别人AI讨论"(wordvisiononly讨论.md) — 待验证的结论
- 实测1个docx得出"batch一定降精度、per-page最准、docx全vision-only可行"。
- **疑点**:单文件孤证;没控制"文本兜底"变量;xlsx上batch反而赢(覆盖90%vs18%)却被忽略。

---

## 课题① batch vs per-page —— 第一轮（2026-06-27）

**测法**:补充回答.docx(4页,含lululemon密集数字表+中文批注) + Excelドリル2.xlsx(30页)。同批页图,4配置真调gemini-3-flash,逐字核对。

### docx 结果（已逐字人工核对，扎实）
| 配置 | 那张lululemon密集数字表(img-005) |
|---|---|
| A_perpage (full,无兜底,每页单喂) | **标(unreadable)整张放弃** |
| A_batch (full,无兜底,4页一喂) | **整张丢失**(标题直接跳下节) |
| B_perpage (full,docling兜底,每页单喂) | **当"图片"一句话描述带过,不逐字转写** |
| **B_batch (full,docling兜底,4页一喂)** | **逐字读全,18,248/847,416/8,110,518零错字** |

- **已排除照抄假象**:docling兜底文本里完全没有这些表格数字(核查过),batch读对=真读图。
- **与word讨论完全相反**:不是"batch降精度",反而batch读全了per-page放弃的表。

### 初步观察（强假设，⚠️ 仅1次1文件，未复测，不可当结论）
1. 决定"密集数字表读没读到"的,**不是batch vs per-page单一维度**,4配置里只有B_batch读到。
2. **新假设**:batch一次看多页,前几页纯文字给了模型"逐字转写"基调,延续到表格页;per-page孤立看一张满是小字的表,易判"这是图,描述带过"而跳过逐字转写。
3. word讨论的"per-page最准"是**换内容类型(密集数字表)就翻盘**的——典型事实≠结论。

### ⚠️ 待排除的假象 / 待办
- [进行中] **随机性**:per-page丢表是否单次抽风?复测per-page×3+batch×3(t1b_repeat.py)。
- [x] ~~xlsx B_batch_real 1165字疑空响应~~ → **已查清,不是丢失**:supplement模式下openpyxl已抓全表格文字,Vision只补视觉增量(图表系列名/坐标轴/黄色输入格/页眉机密标记),输出短是**对的**。
  - **⚠️ 教训(差点犯的事实≠结论错)**:"字数少"是事实,"丢失"是我的直觉结论,人工看全文才知是supplement正常行为。
  - **⚠️ 判据缺陷**:用openpyxl表格文字当探针测supplement输出,必然命中低——因supplement故意不重复表格文字。**xlsx的探针对比方式本身不匹配模式,A组(full)数据才可比,B组(supplement)探针数无意义,要看内容。**
- [x] xlsx A组(都full)逐字核对:**batch把规整数据表读得和per-page一样好**(商品注文票/A店商品情報/課題管理表完整,价格数量都对),**但丢了图表类视觉**(月別売上图表 perpage有batch=0; VLOOKUP perpage有batch=0)。
- [ ] 假设2(基调延续)需更多文件/场景验证。

### 🔑 第一轮综合观察（方向开始复杂化,更要谨慎,⚠️未复测）
**batch好坏不是单一规律,至少两个相反方向:**
- docx密集数字表: **batch读得更全**(per-page整张放弃/当图带过)。
- xlsx图表(月別売上): **batch读得更少**(per-page能补到,batch丢)。
→ 推测纠缠因素:内容是"密集表格"还是"图表"、该格式有无文本兜底、一次喂的页数。
→ **绝不能下"batch更好/更差"的单一结论。** 等随机性复测 + 更多文件再说。

---

## 🚨 课题① 第二轮:复测随机性（2026-06-27）—— 推翻第一轮

针对docx那张密集数字表(12个数字),per-page×3 + batch×3,full模式+无缓存真调。

| | run0 | run1 | run2 |
|---|---|---|---|
| **per-page** | **12/12** | 0/12 | 0/12 |
| **batch** | 0/12 | 0/12 | 0/12 |

### 🔑 关键结论（这个是扎实的:复测得出）
1. **第一轮所有"batch vs per-page"的差异都是单次孤证假象。** 第一轮看到的"batch读全、per-page丢",复测显示**不可复现**——per-page抖动(1次全读2次全丢)、batch这轮3次全丢(与第一轮B_batch读全相反)。
2. **这张密集数字表对两种模式都是"碰运气"**:读不读到主要靠LLM单次随机,**不是batch/per-page的稳定差异**。
3. **方法论验证**:幸亏复测了。若用第一轮单次结果写进设计 = 把噪音当信号。**烧的token值。**

### ⚠️ 这一轮带出的新疑点 / 待办
- [x] batch输出字数波动查清:**不是空响应,是详细度剧烈波动**。run0(2742字)几乎跳过所有英文图片页只读中文评论;run1读一点;run2(8195字)读最全。
  - → **batch对docx的稳定失效模式:一次喂多页时倾向"偷懒/概括",跳过部分页面。不是读错字,是不读。**
- [ ] 密集数字表"碰运气"能否靠 **全分辨率单独喂图** 变稳定?(word讨论说内嵌图全分辨率4/4稳——这可能是真正解法,优先验)
- [x] 既然单次=噪音,后续所有对比**必须≥3次取稳定性**,不能单跑。← 已确立为测试纪律

---

## ✅ 课题① 小结（两轮后,扎实部分）

**对"batch能否扩到全格式"的回答 — 基本否定:**
1. **"per-page一定更准"= 错**。per-page对密集表也抖动(1/3全读,2/3全丢)。word讨论把单次读对账号串当成稳定优势。
2. **"batch一定降精度"= 对一半**。batch对docx有**稳定失效倾向**:输出详细度不稳,倾向跳过/概括部分页(偷懒不读,非读错)。
3. **密集小字数字表对两模式都是难点**,单次靠运气 → 真正的解法可能是**全分辨率单独喂图**(待验),不是batch/per-page之争。
4. **batch在xlsx成立的真因**:不是"batch好",是**xlsx有openpyxl兜底全部表格文字**,batch偷懒也不丢正文(只丢次要的图表视觉)。
   → **印证最初假设:batch安全的前提 = 文本已被可靠抓取。**

**→ 三模式设计取向**:batch保持"仅xlsx长表专用",不作通用加速扩到docx/pdf。

---

## 🚨🚨 课题① 重大方法论修正（2026-06-27 回查源文件时发现）

**回查时发现:之前课题①整个测试测错了对象。典型"测试数据=事实,但'反映真实系统'=未验证的结论"。**

### 漏洞
- 我测的"per-page vs batch"喂的是 **4张LibreOffice整页图**。那张lululemon密集数字表(img-005)在整页图里**被缩小**,所以读不清、碰运气。
- 但**真实pipeline里docx内嵌图根本不靠整页图读**:走 `_enrich_embedded_images`(pipeline.py:2061),**每张内嵌图按原分辨率单独送Vision**,用专门的 `_EMBEDDED_IMAGE_PROMPT`(要求逐字转写表格)。img-005是84KB独立PNG,会单独全分辨率读。
- docling从源docx抽出6张内嵌图(源文件10张,4张0KB空图),img-005=lululemon表,**图序对应已核对无误**。

### 后果
- 课题①得出的"密集表碰运气""batch/per-page都难读这张表"——**反映的是真实系统里不存在的场景**(整页图读那张表)。
- 那张表在真实系统走的是**全分辨率内嵌图路径**,大概率读得稳(word讨论的"内嵌图全分辨率4/4稳"正指这条)。
- ⚠️ **但课题①的别的结论仍有效**:batch对整页正文有"偷懒概括"倾向、per-page抖动——这些是整页图本身的现象,与内嵌图路径无关,依然成立。只是"密集表读不到"这条要作废重测。

### 待办(用真实路径重测)
- [x] **已重测,结论决定性**:全分辨率单独读img-005 ×3次 = **[12,12,12] 完美稳定**(每次~3s,~1000字紧凑)。
  - 对比整页图: per-page[12,0,0] / batch[0,0,0] 碰运气。
  - → **课题①"密集表碰运气"是测错对象的假结论。真实系统用全分辨率内嵌图路径,对这张表处理是完美的。**
  - → 方法论印证:幸亏回查了源文件+源码。否则会把假结论写进设计。

### 🔑🔑 对三模式设计的关键含义(扎实)
**真实docx有两条视觉路径,缺一不可:**
1. 整页图(LibreOffice渲染) → 看布局/页面整体/页内大字。
2. **内嵌图全分辨率单独读**(`_enrich_embedded_images`,专用`_EMBEDDED_IMAGE_PROMPT`) → 看图里小字细节(密集表/账号串)。

**vision_only引擎只做整页图渲染(PyMuPDF渲染页),会丢掉路径2。**
→ vision_only扩docx的**核心风险(已证实,非推测)**:那张密集表会从"3/3完美"退化回"整页图碰运气[12,0,0]"。
→ **这比"LibreOffice慢"更致命。vision_only扩docx前必须解决:怎么保留全分辨率内嵌图路径。**

### 数据留档
- 全分辨率重测: `scratchpad/t1c_run.log` + `scratchpad/t1c_out/embedded_*.md`

---

## 课题② 各格式视觉路径核对（举一反三,纯读源码,2026-06-27）

**问题**:docx发现"全分辨率内嵌图路径"很关键,其它格式有没有?会不会又测错对象?

### 核对结果(源码确认,扎实)
`_enrich_embedded_images`(全分辨率单独读内嵌图)只消费 `metadata["embedded_images"]`,该key只在2处写:
- docx: `_extract_docling_pictures`(docling_parser.py:484,**严格限定docx**)
- xlsx: openpyxl渲染器(docling_parser.py:1764)
- **PDF / PPTX 不写 → 没有全分辨率内嵌图路径!**

### 各格式视觉路径全景

| 格式 | 正文文本兜底 | 整页图(看布局) | 全分辨率内嵌图(看小字) | 整页Vision模式 |
|---|---|---|---|---|
| **docx** | Docling直读OOXML(可靠) | LibreOffice | ✅ 有 | **supplement**(因两兜底都齐,只补漏) |
| **xlsx** | openpyxl直读(可靠) | LibreOffice | ✅ 有 | **supplement** |
| **PDF** | Docling布局模型(会碎表) | PyMuPDF/Docling | ❌ 无 | **full**(每页全转写) |
| **PPTX** | Docling布局模型(会碎表) | LibreOffice | ❌ 无 | **full** |

### 🔑 深层设计真相(config 656-668行揭示)
**docx/xlsx能用supplement(省钱)的前提 = 两个可靠兜底都齐**:正文直读OOXML(不碎) + 图全分辨率单独读。
**PDF/PPT必须full(贵) = 两个兜底都没有**:布局模型碎表 + 无全分辨率内嵌图路径。
→ **这不是随意配置,是被"有没有可靠兜底"决定的。三模式设计必须尊重这个约束。**

### 对"测错对象"风险的结论
- docx那次测错(用整页图测内嵌图内容)是**docx特有**——因为只有docx/xlsx有"内嵌图≠整页图"的双路径。
- **PDF/PPT不存在这个陷阱**:它们本来就只有整页图一条视觉路,我之前没测PDF/PPT,但若测,整页图就是真实路径,不会测错对象。
- ⚠️ 但PDF有另一条专属路径没测:**text_layer**(pymupdf直读PDF嵌入文字层,喂full-mode Vision当"权威字符参考")。PDF的细节准确性可能靠这条,测PDF时要带上。

### 对三模式设计的硬约束(扎实)
1. **supplement/full不是可随意切的旋钮**,是格式能力决定的:docx/xlsx有兜底才敢supplement,PDF/PPT没兜底必须full。
2. **vision_only对docx/xlsx危险**(丢全分辨率内嵌图路径+丢OOXML正文兜底→supplement的两个前提全没了,被迫退回整页碰运气)。
3. **vision_only对PDF天然合理**(PDF本就无内嵌图路径、full模式,vision_only只是把"Docling整页文本"换成"纯靠图",损失相对小)——这也解释了为何现状vision_only只做PDF。

### 待办
- [ ] PDF 的 text_layer 路径实测:它对PDF细节准确性贡献多大?(关系vision_only-PDF会损失什么)
- [ ] PPTX 实测:无兜底+full,现状质量如何?(2个pptx样本:前桥学习Agent.pptx 等)

---

## ✅ 课题③ vision_only扩docx的代价（推断→事实,2026-06-27）

对比同一docx,A现状完整pipeline vs B模拟vision_only(只整页图full转写,无正文兜底+无内嵌图路径),各×3,已人工核对。

| | 密集数字表(img-005小字) | 整篇大字内容 |
|---|---|---|
| **A 现状**(有全分辨率内嵌图路径) | **[12,12,12] 完美稳定** | [15,15,15] |
| **B vision_only**(只整页图) | **[0,0,11] 抖动崩坏** | [15,15,15] |

**人工核对**:B run0把那张表当`![...page screenshot](attachment)`一句话带过(没读数字);B run2碰运气逐字读到。→ [0,0,11]真实,整页图模式读密集表全凭运气。

### 🔑 钉死的事实
1. vision_only扩docx**不是全面崩**:整篇大字/正文/标题都不丢(15/15)。
2. **崩的是密集小字细节**(数据表/图内小字):现状靠全分辨率内嵌图路径稳定[12,12,12];vision_only退回整页图后→当截图带过,读不读全凭运气[0,0,11]。
3. **根因=之前推断**:vision_only丢了全分辨率内嵌图路径。**推断=事实,已验证。**

### 对三模式设计的硬结论
- vision_only扩docx**有明确代价,且代价可界定**:丢"图里密集小字",不丢整篇内容。
- 取决于文档类型:**纯文字/大字docx → vision_only没问题;含密集数据表截图的docx → 丢数字**。
- **可行的设计方向**:若要vision_only安全扩docx,**保留全分辨率内嵌图路径**(整页走vision,但内嵌图仍单独高清读)。这是具体的、已被数据支持的方向。

### 数据留档
- `scratchpad/t2_visiononly_docx.py` + `scratchpad/t2_out/{A_real,B_visiononly}_*.md` + `t2_summary.json`

---

## ⚠️ 课题④ PDF text_layer — 第一轮判据缺陷（2026-06-27,又一次"事实≠结论"）

测契約書PDF p1(有红章/SIGN遮挡): A现状(有text_layer) vs B vision_only(无text_layer),各×3,12个被遮挡/密集字段。

**结果反直觉**: A=[7,7,7], B=[7,9,8] —— B(无text_layer)反而略好?!

### 深查后真相(人工核对+源码)
- 那5个稳定漏字(奥田/範夫/東陽投資/053248/189626)**全在text_layer里且正确**,但A组也漏。
- 看A组输出:整个仲介者署名区被Vision概括成一句**「署名印あり」**跳过了(不是看错,是主动跳过签章区)。
- **根因**: text_layer的authoritative机制设计用途 = **解决视觉冲突**(图上像「典」但text_layer是「良」→信text_layer),**不是**补全Vision主动跳过的区域。Vision跳过签章区,text_layer不管。

### 🔑 两层发现
1. **测试缺陷(我的错)**:选错判据。"被遮挡字段"测不出text_layer价值——因为这些字是被Vision**当签章区跳过**(非看错),而text_layer只管"冲突"不管"跳过"。**A=B ≠ text_layer没用,而是没测到它起作用的场景。**
2. **真实系统短板(有价值)**:契約書签章区(登録番号/取引士名),现状和vision_only**都**会概括跳过读不到。与text_layer无关,是Vision prompt对"署名区"的倾向。

### ✅ 课题④ 结论（决定:承认测不出text_layer价值,记下已确认事实往下走）
**已确认的事实(够用):**
1. text_layer的作用域很窄 = **仅"视觉冲突纠错"**(图上看错X、text_layer正确Y时,信text_layer)。**不补**Vision主动跳过的区域。
2. 现有样本测不出其价值:契約書签章区是被"跳过"(非"看错"),不在text_layer作用域内。这种"视觉冲突"页在现有test_docs里不常见。
3. **vision_only-PDF的text_layer损失 = 仅限"视觉冲突纠错"这个窄场景**,常见场景(读字/跳过签章)它不比现状差。
4. **对三模式的意义**:vision_only-PDF损失的是一个**窄而真实但不高频**的能力(印章/模糊遮挡下的字符纠错)。设计时知道这个代价存在即可,不是阻断性问题。
- 数据留档: `scratchpad/t4_*`

> ⚠️ 元观察:课题①(docx内嵌图)、④(PDF text_layer)连续两轮都是"表面数据→深查→发现测错对象/判据"。**"事实≠结论"在本项目反复奏效,每次拦住一个假结论。** 后续任何反直觉结果先深查,别信表面。

---

## 课题⑤ PPTX（测中,2026-06-27）

### 选样阶段又抓到一次"差点测错对象"
- 第一版探针用前桥pptx的ReAct流程图文字(Thought/Action/思考と行動の反復…)→ **核对发现全在docling文本里**。
- **根因(pptx重要事实)**:pptx的"图"多是**PowerPoint原生形状/SmartArt**,文字是真文字,**docling能直读**(不像PDF/docx图是纯像素)。
- 前桥pptx的63个位图 = **无字装饰图标**(键盘鼠标/云服务器icon)→ 也测不出读图。
- 改用 **git_presentation_sample.pptx**:有真正的**含字位图**(SVN vs Git对比图,CENTRAL REPOSITORY/only works when connected等),已核对docling抓不到→正确样本。

### pptx 视觉内容三分类(重要,影响设计)
| 类型 | docling能否抓 | 例 |
|---|---|---|
| PowerPoint形状/SmartArt文字 | ✅ 能直读 | 标题、流程图节点、正文 |
| 无字装饰位图 | N/A(无字) | 图标、照片 |
| **含字位图** | ❌ 抓不到,只能读图 | 截图、外部插入的对比图 |

→ pptx现状质量**大部分靠docling形状文字(可靠)**,整页图Vision只在"含字位图"场景有增量价值。这类位图多不多,决定pptx读图的重要性。

### ✅ 结果(已人工核对)
git_presentation含字位图(SVN/Git对比图) ×3: **[6, 0, 4] 剧烈抖动**。
- run0(6/6):图被完整逐字读出(LOG/REVERT/CENTRAL REPOSITORY/only works when connected全有)。
- run1(0/6):同一张图,Vision只用一句`![GitHub logo](image)`概括带过,没逐字读。
- run2(4/6):部分读到。
→ **pptx含字位图的读取靠单次随机:有时逐字读、有时一句话概括跳过。**

### 🔑🔑 跨格式统一规律（三个格式指向同一结论,这才扎实）
| 格式 | 场景 | 现象 |
|---|---|---|
| docx | 密集数字表(整页图模式) | [12,0,0] 抖动:有时读全有时当截图带过 |
| PDF | 契約書签章区 | 概括成「署名印あり」跳过 |
| pptx | 含字位图(SVN/Git图) | [6,0,4] 抖动:有时逐字有时一句话带过 |

**统一根因**:整页图模式下,Vision面对"一张内容密集/像独立图的区域"时,**是否逐字转写全凭单次随机**——有时认真读,有时判定"这是张图,描述一下就行"概括跳过。

**这是DocIngest最核心的视觉短板**(跨所有格式),也解释了为何docx/xlsx要专门做"全分辨率内嵌图路径"(把图单独抠出来,用强制逐字转写的`_EMBEDDED_IMAGE_PROMPT`,实测docx那张表[12,12,12]完美稳定)。

### 对三模式设计的总纲(扎实,三格式互证)
1. **"全分辨率单独读图 + 强制逐字prompt"是治这个抖动的解药**(docx/xlsx已用,实测稳定)。
2. **PDF/pptx没有这条路径** → 它们的含字图/密集区读取本就不稳(现状短板,非vision_only独有)。
3. **vision_only让情况更糟**:它退回纯整页图,把docx/xlsx好不容易建立的"全分辨率路径"也丢了 → 抖动从docx/xlsx蔓延回来。
4. **三模式真正的质量差异轴 = "对密集/含字图区,有没有强制逐字读取的保障"**:
   - 默认/triage关:docx/xlsx有全分辨率路径(稳),pdf/pptx无(本就抖)。
   - vision_only:全格式退回整页图(全抖) ← 这是它最大的质量代价,不止"慢"。
- 数据留档: `scratchpad/t5_*`

---

## 🎯 病根定位:是PROMPT问题（2026-06-27,用户点破）

对比稳定的`_EMBEDDED_IMAGE_PROMPT`(那张表[12,12,12]) vs 抖动的`_PAGE_PROMPT`(整页[12,0,0]):

| prompt | 对"表格/含字图块"的措辞 | 结果 |
|---|---|---|
| `_EMBEDDED`(全分辨率抠图) | 第一句定性"a table/chart/screenshot",**第一条规则"If TABLE→output full markdown table, every cell verbatim"** | 稳定逐字 |
| `_PAGE`(整页full) | 只有flowchart规则(读节点标签),**没有"页内表格/含字图必须逐格抄"的硬命令** | 抖动:把密集表当"一张图"概括跳过 |

**病根**:`_PAGE_PROMPT`缺一条"页面内嵌的数据表/含字图块,必须像独立表格那样逐格逐字转写,禁止用一句话概括成'此处有截图'"的硬命令。`_EMBEDDED`有,所以稳。

→ **统一prompt的方向**(⚠️ 此方向后被推翻):当时设想把`_EMBEDDED`硬命令移植进`_PAGE_PROMPT`。t6/t7验证后证明prompt治标不治本(见下),最终走抠图路径。

### ⚠️ t6验证结果:这轮prompt加强 —— 实测无效（诚实记录,2026-06-27）
| 样本 | 原版 | 加强版 |
|---|---|---|
| docx那张表(整页图) | [0,0,0] | [0,0,0] |
| pptx含字位图 | [4,6,6] | [6,6,4] |

- **人工核对**:加强版那张表仍被处理成`![lululemon impact report excerpt]`一句话带过,没逐字读。硬命令没改变行为。
- ⚠️ 注意:t6 docx原版[0,0,0] vs 之前t2的[0,0,11] = 纯LLM随机(整页图读那张表6次仅1次中,基本靠运气)。docx是**地板效应**,测不出prompt改善空间。

### 🔑 失效根因分析(指向更深的结论)
1. **插入位置问题**:HARD_RULE插在Decision logic前,但Decision logic里`screenshots → flowchart rules`那条偷懒口子还在,可能盖过硬命令。
2. **更本质**:那张表对docx是**整页里一小块**(浅绿淡字配图),Vision把它当"页面一角的配图"而非"必须逐字的表"。`_EMBEDDED`稳是因为**单独看整张就是表**,无"这只是配图"的语境稀释。

### 🔑🔑 修正后的结论(重要)
**问题可能不主要是prompt措辞,而是"整页图"这个输入形式本身的局限** —— 表在整页里被当配图,再狠的prompt也难扭转。
→ **这反而强力支持"全分辨率单独抠图路径"才是真解药**(_EMBEDDED实测[12,12,12]稳定)。
→ **课题三的推荐要修正**:不是"先靠prompt治本",而是**"全分辨率内嵌图路径"是PDF/PPT也该补的硬通货**(PPT已于t8补上)。prompt加强不能替代单独抠图。
→ ✅ 已验证(t7):prompt加强微弱正效但不显著(37.5%→50%),治标不治本。抠图路径才是解药。

### ✅ t7返工测试:修正判据(归一化+加样本×8)后的可信结论（2026-06-27）
修正t6两缺陷:探针归一化(忽略大小写/格式,CENTRAL=central)、同页×8压方差。docx第3页(确含lululemon表)单页对照,v2原地改口子prompt。

| | 8次命中分布 | 命中率(满分次数) | 均值 |
|---|---|---|---|
| 原版 | [0,0,0,0,0,12,12,12] | 3/8 = 37.5% | 4.5 |
| v2加强 | [0,0,0,0,11,12,12,12] | 4/8 = 50% | 5.9 |

(已重算复核+人工抽查:满分次是真逐字读成markdown表、无幻觉,还诚实标了读不清的`112.?`)

### 🔑🔑🔑 可信结论(数据扎实)
1. **双峰分布**:读那张表是**二元随机决策** —— 要么全读到(11-12)、要么全丢(0),几乎无中间值。Vision每次随机决定"逐字读"还是"当配图带过"。
2. **prompt加强:微弱正效,但不显著**。命中率37.5%→50%(8次仅多1次),在噪声范围内,**不能算有效**。
3. **核心问题没解决**:即便加强版,**仍50%概率整张表全丢**。生产不可接受。
4. **根因坐实**:整页图形式下,"这块要不要逐字读"是Vision的随机二元选择,**prompt只能轻微偏置概率,无法消除"带过"分支**。

### → 最终设计结论(三模式核心,扎实)
- **prompt调优治标不治本**:能微调概率,消不掉50%全丢的尾部风险。
- **`_EMBEDDED`抠图路径才是真解药**:[12,12,12]稳定,因为它**不给"这是配图"的选项**——整张图就是转写对象,无二元决策。
- **三模式质量保障的硬通货 = 全分辨率单独抠图 + 强制逐字prompt**,不是改整页prompt。
- **下一步真正该验**:抠图路径能否扩到PDF/PPT(它们现在没有)。这是治本方向。prompt微调可作辅助锦上添花,但不单独依赖。
- 数据留档: `scratchpad/t7_*`

### ✅✅ t8验证:PPT放开抠图入口 —— 治好抖动(方案成立,2026-06-27)
核实链:PPT的SVN/Git图=独立位图(ppt/media/9个PNG),docling解析进doc.pictures=11个**带PIL数据**(has_pil=True),
现有`_extract_docling_pictures`格式无关、pptx markdown有11个`<!-- image -->`占位符一一对应、调用点(pipeline:2396)无格式门。
→ 唯一卡点:docling_parser.py:473的`if suffix==".docx"`硬编码。

实测(monkeypatch放开,抠图路径对那张图×8,归一化探针):

| | 8次 | 基线(t5整页图) |
|---|---|---|
| 抠图路径 | [6,6,6,6,6,6,6,6] **零抖动** | [6,0,4] 50%全丢 |

人工抽查run0:把SVN/Git对比图转写成完整markdown表(SUBVERSION/GIT两列,CENTRAL/REMOTE REPOSITORY/PUSH/PULL/FETCH/WORKING COPY/LOCAL REPOSITORY全在,注释也读到),**真逐字、无幻觉**。

**结论**:放开抠图入口到pptx → 含字位图从"50%全丢"变"8/8稳定读全"。和docx[12,12,12]同理。方案成立,落地源码。
- ⚠️ 矢量PDF不走这条(文字层本就有);含位图PDF需pymupdf自抽(docling has_pil=False),优先级低。
- 数据留档: `scratchpad/t8_*`

### ✅ 已落地源码 + 端到端验证(2026-06-27)
3处改动(最小侵入):
1. `docling_parser.py:473` 抠图入口格式门 docx → docx+pptx(PDF排除:has_pil=False无像素)
2. `_extract_docling_pictures:1374` 配置键 `parsing.docx.*` → 按后缀 `parsing.{suffix}.*`
3. `config/default.yaml` pptx块加 image_extraction(enabled/min_dimension/vision_enrich/max_images_vision)

端到端实跑改过源码(非脚本monkeypatch):
- pptx那张SVN/Git图(img-008,1010x1014)经`<!-- vision-enriched image=...-->`注入,SUBVERSION/GIT完整逐字转写,探针6/6 ✓
- 仓库无该路径单测→手动跑docx回归:命名marker=6/enriched注入=6/lululemon表探针6/6,**和改前一致,没破坏** ✓

### ✅✅✅ 全面验证收口(2026-06-27)
| 验证项 | 结果 |
|---|---|
| 守门单测 test_index_file_level_fields | 2 passed ✓ |
| 路由:pptx前桥 | embedded=65(送58) 新放开生效 ✓ |
| 路由:docx | embedded=6(送6) 原样不变 ✓ |
| 路由:nutrition.pdf(矢量) | embedded=0 正确排除 ✓ |
| 路由:BehaviouralInsights.pdf | embedded=0 正确排除(has_pil=False) ✓ |
| pptx完整pipeline(前桥,真调) | 抠图注入30+整页enriched11 共存不打架 ✓ |
| 语法/yaml | OK ✓ |

⚠️ **遗留观察(非bug)**:前桥pptx有58张够大的图,默认`max_images_vision:30`跳了28张(warn了,assets都存,可调大)。
PPT每页常多图,30可能偏小——但这是照搬docx/xlsx现有设计,且符合"warn+可调"非静默截断。三模式设计时可重新评估PPT的cap默认值。

### ✅ 后续:去掉PPT抠图静默截断 + 修成本预检漏算(2026-06-28)
用户要求:别用`max_images_vision:30`硬截断(违反反过度防御"事后截断伪装成本防御"),改"全量+事前知情"。

**发现的真bug(查源码时)**:
1. 代码默认本就"不设上限"(`max_n=None→读全`)。截断是我引入的(默认yaml给pptx写了30)。
2. **safety成本预检双重漏算PPT抠图**:(a)`inspect._inspect_pptx`没数`ppt/media/`图;(b)`safety.estimate_file_cost_usd`写死读`parsing.docx.*`配置。→ PPT抠图成本(最贵部分)对事前预检**完全不可见**,用户会被看不到的账单偷袭。

**4处改动**:
1. `default.yaml` pptx `max_images_vision: 30→null`(不设上限)
2. `inspect.py` 加`_count_pptx_media()`(数ppt/media/图片,排除音视频)
3. `inspect.py._inspect_pptx` 返回加`media_files`
4. `safety.py` 成本预检按`info["format"]`取配置(docx/pptx各算各的),不再写死docx

**验证**:前桥pptx预检成本$0.806,其中**$0.65(80%)来自抠图**——修复前这$0.65对预检不可见,现在事前可见可审批。docx预检不变($0.182)。PDF不算embedded($0)。回归33 passed,守门测试过。
→ **这才是"加警告"的正解**:用现有safety事前预算机制让用户知情/审批,不是事后截断、也不另造轮子。符合CLAUDE.md"事前算budget让用户审批"。

### 全量抠图墙钟实测 + 装饰图问题(2026-06-28,前桥58图)
| | 基线(只整页图) | 改后(全量抠58图) |
|---|---|---|
| 墙钟 | 31.4s | **27.1-27.4s(没变慢)** |
| 抠图注入 | 0 | 58/58 两轮稳定 |
| markdown字数 | 4519 | 17433-18134(≈4倍) |

**慢多少?不慢** —— 抠图调用和整页图并发重叠,加的是"API调用次数(成本)"不是墙钟。11图/58图两样本一致,可下定论。

**但抽查发现:58图里30张(52%)是无字装饰图标**(放大镜/键盘/机器人icon),白花API读出`[unreadable]`噪声。有效率48%。

### 决策:装饰图过滤 + 后过滤 —— 都不做,保留现状(2026-06-28)
参考现有机制`_detect_furniture_pictures`(家具图检测)后定的:
1. **不做送Vision前的无字图预过滤**:现有furniture检测判据是"跨页重复"非"无字",且默认OFF(opt-in)。本地OCR判"有没有字"有误差→误杀含浅色小字的关键图。违背项目"false positive丢信息,比多花一次Vision糟得多"的保守哲学。
2. **不做后过滤(无字结果不注入)**:动手前抽查真实数据发现——`vision-enriched`段里`[unreadable]`声明常**和图旁日文正文混在一段**(質問・検索/翻訳等),简单删整段会误删正文。边界太微妙,误删风险不值得。
3. **保留现状的依据**:正确性OK(该读的100%读到、无字图老实[unreadable]不幻觉);成本已被safety预检事前全额定价、可审批;`[unreadable]`噪声下游RAG可自行过滤。
→ 三模式设计时若不同模式对成本/质量取舍不同,可再统一考虑(如极致省成本模式才开预过滤)。当前不急。

---

# 三模式整体方案设计(2026-06-28 开始)

## 核心洞察:三模式是"面向场景的预设档",不是一个轴的三档
用户最初的"默认/triage关/vision_only"其实混了两个正交维度:
- **维度A(engine)**:用哪个解析后端 — docling / vision_only / azure_di
- **维度B(triage)**:docling内要不要跳纯文字页省Vision钱

"triage关"动B,"vision_only"动A,不在一个轴上。→ 重构为**3个面向场景的预设档**,每档把A/B+其他旋钮调成一套合理组合。用户选场景,不手动调旋钮。

## 三档定义(已与用户确认)

### 平衡档(默认) — 日常首选,成本质量平衡
| 旋钮 | 取值 | 依据 |
|---|---|---|
| engine | docling | 解析准+正文兜底 |
| triage | 开 | 纯文字页跳Vision省钱 |
| 抠图(docx/xlsx/pptx) | 开 | 质量地基,治含字图抖动(已落地) |
| batched | xlsx开/其他关 | batch只在xlsx成立(openpyxl兜底) |

### 准确档 — 质量优先,不在乎多花钱
| 旋钮 | 取值 | 依据 |
|---|---|---|
| engine | docling | 同上 |
| triage | **全局关(每页都送Vision)** | 用户确认:零漏页风险,token翻倍换"绝不漏" |
| 抠图 | 开 | 同平衡 |
| batched | 全关 | 准确档不要batch偷懒风险,一律per-page |

### 速度档(vision_only) — docling慢,牺牲可界定精度换高速
| 旋钮 | 取值 | 依据 |
|---|---|---|
| engine | vision_only | 跳docling解析,整页Vision,OOM免疫 |
| 适用格式 | PDF/图片 + **扩PPT(用户确认这轮做)** | 不走docling,PPT同理可渲整页图 |
| 抠图 | 不适用 | vision_only不解析,只有整页图 |
| 并发 | 拉高(64) | 速度档高并发猛冲 |

## 用户入口:还没想好(待定),先做实现

## ⚠️ vision_only扩PPT的设计现实(实测中)
读源码发现:**不能简单把.pptx加进`_PAGE_IMAGE_FORMATS`**。
- PDF走`_parse_pdf`用PyMuPDF渲染(快/无OOM),是vision_only的卖点。
- **PPT没有PyMuPDF渲染路径**,必须LibreOffice→PDF→截图(慢)。加进去会进错分支崩。
- 扩PPT需新写`_parse_pptx`渲染路径。**且vision_only-PPT省不掉LibreOffice渲染那步慢的**——vision_only对PDF的"省时间"卖点,对PPT可能打折扣。
- → 正在实测:vision_only-PPT到底比docling-PPT快多少(决定这个设计成立不成立)。

## ✅ vision_only-PPT:放弃(实测拍板,2026-06-28)

> 由"搁置待查"升级为"放弃"。先前归因("PPT无PyMuPDF快渲染器")**不准确**,已用耗时拆解纠正:慢的是LibreOffice→PDF那步,不是渲染——而那步vision_only也躲不掉。

### 🔑 决定性数据:PPT 处理耗时拆解(两样本,真实计时,不烧Vision)
| 样本 | docling解析<br>(vision_only能省的) | LibreOffice→PDF + 渲染<br>(vision_only也躲不掉) | Vision@64并发 |
|---|---|---|---|
| git_presentation(10页9图) | **6.9s** | 3.2+1.2 = 4.4s | ~3s(21次/1波) |
| 前桥(12页50图) | **2.5s** | 6.7+1.5 = 8.2s | ~6s(77次/2波) |

(数据留档:`scratchpad/ppt_timing.py`。MuPDF "structure tree" 警告不影响渲染,PNG全出。)

### 🔑🔑 三条钉死的结论
1. **vision_only-PPT省的是最小一步,被自己躲不掉的成本盖过**。PPT不是PDF,**必须LibreOffice→PDF才能渲染**,这步(3-7s)vision_only躲不掉。它和docling路径在"渲染+Vision"两步完全一样,只差一个docling解析。前桥:省解析2.5s,仍付LO+渲染8.2s——省的比留的还少。
2. **docling解析本就便宜(2.5-6.9s),还白送高质量形状文字**。前桥解析仅2.5s却出6342字形状文字(docling直读PowerPoint文字框,零API零误差)。vision_only扔掉它=省2.5s+丢全部正文质量+改用AI看图猜字,纯亏。
3. **"64并发"不是vision_only独家**(源码坐实):per-page Vision并发由`performance.parallel_files`控制,`_enrich_with_vision`是docling/vision_only**共用的Phase 1.5**。docling-PPT设`parallel_files=64`一样1-2波冲完(前桥77次@64=~6s)。vision_only唯一真省的只有"跳docling解析"本身。

### 真正的瓶颈(速度档该优化的)
前桥总时间(docling路径,64并发):解析2.5 + LO 6.7 + 渲染1.5 + Vision 6 ≈ **17s**。最大两块是**LibreOffice(6.7s)和Vision(6s),不是docling解析(2.5s)**。vision_only砍的恰是最小那块还附带质量损失。前桥77次Vision里65次是抠图——砍这块(激进triage/评估抠图必要性)收益远大于换引擎。

### 自研/找库 — 都不行(2026-06-28)
- **找开源快渲染库:失败**。能跳过LibreOffice的只有商业库(Aspose加水印/Spire限页),其余底层全是LibreOffice。
- **自研PPT→图渲染器:不划算**。PPT存的是"排版指令"(OOXML)不是"成品坐标",渲染要重写半个PowerPoint排版引擎(187+预设形状/7层样式继承/主题色/SmartArt),几人年量级,与开源生产定位冲突。PyMuPDF快是因为PDF是"已排好版"格式,PPT没这便宜可占。

### → 速度档对PPT正解(已被数据支持)
**docling + parallel_files=64(高并发) + 激进triage(多跳纯文字页) + 关抠图**——保留形状文字质量,并发拉满冲Vision,靠少送页/少抠图省时省钱。**不换引擎**。每格式选最快路径(PDF用vision_only / PPT用docling高并发),而非教条全用vision_only。

---

# 质量地基实测:抠图相关三忧虑(2026-06-28)
围绕"抠图是质量地基"展开的三个验证,逐项实测+肉眼核对,排除假绿灯。

## ① DPI 救不了抖动 —— 决定性否证
**假设**:适当调高DPI,不靠抠图也能稳定逐字读密集表。
**实测**(2样本×4档×8次=64次真调,逐次肉眼核对,数据留档`scratchpad/dpi_out2`):

| 样本 | 180(3MP) | 400→4MP | 400→8MP | 600→34.8MP超清 |
|---|---|---|---|---|
| docx密集数字表 | 2/8 | 2/8 | 3/8 | **2/8** |
| pptx SVN/Git图 | 7/8 | 5/8 | 7/8 | 8/8 |

- **docx钉死结论**:拉到34.8MP超清(11倍像素),满分率还是2/8,和180一样。肉眼核对:同一张超清图,有时逐字读全(无幻觉)、有时标`![](image)`整块跳过——**二元随机决策,与分辨率无关**。32次只出现0/6无中间值=真双峰。
- pptx的7/8→8/8是噪声(中间400还掉到5/8,不单调)。
- **自查排除假绿灯**:底层`describe_image`无结果缓存(8次独立真调)、无内部压图(原图base64直传,34.8MP原样到Gemini)、探针无漏判中间值、页码经文字层+肉眼定位无测错对象。

**业界共识**(arxiv 2503.23667 实测GPT-4o/Gemini + 多源):<150DPI明显劣化 / **~200最平衡** / 300追平传统OCR / 400+收益递减token暴涨。
**含义**:项目默认180略低,可选提到200(锦上添花,非解决抖动)。300以上对抖动无用只烧token。
**⚠️ 4MP上限是我们代码的保守硬编码,非模型红线**(Gemini吃34.8MP无碍);留着当成本护栏合理,但注释理由该改成"成本控制"而非"模型拒收"。

## ② 抠图并发 —— 默认16,安全
`_enrich_embedded_images`用`performance.parallel_files`(config默认**16**),和per-page Vision共用worker池+全局闸`_VISION_GATE`,两道闸串联不压垮provider rate limit。

## ③ 注入位置 —— 准确(完整pipeline实测)
git_presentation(11图)完整pipeline真跑+肉眼通读markdown:
- 11张图转写**全部锚定到对应marker位置**,内容无张冠李戴(img-008=SVN/Git对比表逐字读全 / img-002=Octocat / img-009=mascot,核对无误)。
- **图序=页序=注入序**,严格按出现顺序;整页Vision块与抠图块穿插共存各就各位;`unanchored=0`(无图走兜底)。
- 机制:positional marker锚定 + 反向注入防漂移 + 找不到marker追加文末(绝不丢)。数据留档`scratchpad/inject_kb`。
- 小瑕疵(非bug):`<!-- vision-enriched -->`(整页)和`<!-- vision-enriched image=...-->`(抠图)视觉上挨着,人读markdown不易分辨段落归属;不影响RAG/grep(内容都对都在对位),纯可读性。

## 本轮三模式设计收口状态(2026-06-28)
**已定**:两正交维度认知(engine/triage)、三预设档定位(平衡/准确/速度)、平衡档+准确档的旋钮组合、质量地基(抠图覆盖docx/xlsx/pptx)+成本预检、**xlsx抠图上限统一(30→null,成本进预检)**、**vision_only-PPT放弃(实测拍板)+速度档对PPT正解=docling高并发**。
**待查**:① 速度档对docx/xlsx的具体路径(docx有LibreOffice同样开销,大概率同PPT结论);② 用户入口形式(mode参数 vs 纯文档);③ 速度档`parallel_files=64`的并发上限/rate limit实测。
**下轮可做**:实现速度档(docling+高并发+激进triage的config预设),或定用户入口。

> 📌 早期"设计方向"章节(含"prompt统一治本""vision_only扩PPT成立")已被后续测试推翻,已删除。最新结论以本文件顶部「最新结论速查」+「三模式整体方案设计」为准。
